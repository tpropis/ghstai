version: "3.9"

services:
  proxy:
    build: ./services/proxy
    ports:
      - "5000:5000"
    restart: unless-stopped

  dashboard:
    build: ./services/dashboard
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - NEXT_PUBLIC_PROXY_BASE=https://didactic-space-computing-machine-x564v6qwg5j6f6wqq-5000.app.github.dev
